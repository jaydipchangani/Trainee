import { Layout, Menu } from "antd";
const { Header } = Layout;

const AppHeader = () => (
  <Header style={{ background: "#001529", color: "white" }}>Job Portal</Header>
);
export default AppHeader;
