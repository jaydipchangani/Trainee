import React from "react";
import { Typography } from "antd";

const Group: React.FC = () => {
  return (
    <div style={{ padding: "20px" }}>
      <Typography.Title level={2}>Group Page</Typography.Title>
      <p>This is the Group page where group-related information will be displayed.</p>
    </div>
  );
};

export default Group;
